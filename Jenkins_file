pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                git 'https://github.com/ArtiomIshkov/MySoftware.git'
                sh 'ls -ltr'
            }
        }
    }
}
